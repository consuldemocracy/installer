---
- name: Create deploy user
  hosts: all
  become: yes
  roles:
    - user

- name: Setup CONSUL
  hosts: all
  remote_user: deploy
  gather_facts: yes
  roles:
    - system
    - postgresql
    - ruby
    - rails
    - ssl
    - email
    - queue
    - unicorn
    - nginx
    - memcached
    - timezone
    # - capistrano
